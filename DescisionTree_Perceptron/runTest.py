from imports import *
import dt

data = array([[1,2,3,4,5],[3,4,5,6,7]])
x1= data[0]
x2= data[1]
dist = 0.0
d=
for d in range(len(x1)):
    dist += (x1[d] - x2[d]) * (x1[d] - x2[d])
print("the or dist = ", + sqrt(dist))

a = sqrt(((data[n][:d]-data[m][:d])*(data[n][:d]-data[m][:d])).sum())
print('the latter dist = ' + a)
